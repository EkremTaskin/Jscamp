export default class User {
    constructor(id, name, lastName, age, city) {
        this.id = id
        this.name = name
        this.lastName = lastName
        this.age = age
        this.city = city
    }
}